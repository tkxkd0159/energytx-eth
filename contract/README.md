# Settings

```
npm uninstall -g truffle
npm install -g truffle

npm i

truffle init

```

## Deploy contract
```bash
truffle compile
truffle migrate [--reset] --network goerli  # compile + deploy
```